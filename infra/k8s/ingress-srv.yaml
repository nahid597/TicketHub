# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: ingress-service
#   annotations:
#     nginx.ingress.kubernetes.io/rewrite-target: /api/users/?(.*)
#     nginx.ingress.kubernetes.io/configuration-snippet: /api/users/?(.*)
#     nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
#     kubernetes.io/ingress.class: public
#     nginx.ingress.kubernetes.io/use-regex: 'true'
# spec:
#   rules:
#     - host: ticketing.dev
#       http:
#         paths:
#           - path: /api/users/?(.*)
#             pathType: prefe
#             backend:
#               serviceName: auth-srv
#               servicePort: 3000

# --- 
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-service
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/use-regex: 'true'
spec:
  rules:
  - host: ticketing.dev
    http: 
      paths: 
      - backend:
          service:
            name: auth-srv
            port:
              number: 3000
        path: /api/users/?(.*)
        pathType: Prefix